fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

langgraph==1.2.10
langgraph-checkpoint-postgres==3.1.0
psycopg[binary]==3.3.4
psycopg-pool==3.3.1

groq==1.6.0

vnstock==4.0.5
pandas==2.3.3
numpy==2.2.6
pandas_ta==0.4.71b0

rapidfuzz==3.14.5
python-dateutil==2.9.0.post0

# v1: local sentiment (news) + embeddings (semantic cache).
# torch is installed separately in the Dockerfile (dedicated CPU wheel index) --
# mixing --extra-index-url into this file makes pip re-check that index for
# every package here too, which was making the build take forever.
transformers==5.14.1
sentence-transformers==5.6.1
pgvector==0.5.0
