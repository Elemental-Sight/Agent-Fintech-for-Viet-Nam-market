# Groq
GROQ_API_KEY=
GROQ_MODEL=qwen/qwen3-32b

# Postgres (service name "db" when run via docker-compose)
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=vn_agent
POSTGRES_USER=vn_agent
POSTGRES_PASSWORD=change_me

# FastAPI backend
APP_HOST=0.0.0.0
APP_PORT=8000

# Frontend (Next.js)
BACKEND_URL=http://app:8000

# Entity resolver data
TICKER_MAPPING_PATH=data/tickers.json
